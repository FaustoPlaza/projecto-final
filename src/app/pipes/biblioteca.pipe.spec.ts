import { BibliotecaPipe } from './biblioteca.pipe';

describe('BibliotecaPipe', () => {
  it('create an instance', () => {
    const pipe = new BibliotecaPipe();
    expect(pipe).toBeTruthy();
  });
});
